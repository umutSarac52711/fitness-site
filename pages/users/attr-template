All attributes:
    created_at (datetime, current_timestamp, nullable)
    email (UNI, varchar)
    id (PRI, int)
    is_active (tinyint, 1)
    name (varchar)
    password_hash (varchar)
    role (enum, 'member','trainer','admin')

Attributes to show in the interface, in order:
    name (varchar)
    email (UNI, varchar)
    created_at (datetime, current_timestamp, nullable)
    role (enum, 'member','trainer','admin')
    password_hash (varchar)
    is_active (tinyint, 1)
    

